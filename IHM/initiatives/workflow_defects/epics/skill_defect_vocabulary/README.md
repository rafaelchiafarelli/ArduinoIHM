# Epic: skill_defect_vocabulary

Add "Defect" to the `workflow` skill's Vocabulary section, plus the one
branch-collision rule that came up putting it into practice. See the
initiative README for the two concrete gaps this closes.

## Tasks

```
1-add-defect-concept   edit SKILL.md   (no deps)
```

## Acceptance gate

The next time a defect is found mid-session, the skill (not Rafael,
live) should answer: is this a Defect or a normal initiative? Does it
need epics/tasks branches now? What happens if those container names
collide with an active chain in the same clone?
